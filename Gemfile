# frozen_string_literal: true

source "https://rubygems.org"

gem "parallel"          # paralelismo via processos ou threads
gem "concurrent-ruby"   # estruturas de concorrência (futures, thread pools, etc.)
gem "benchmark-ips"     # medição de performance